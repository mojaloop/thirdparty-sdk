description: Linking request consent pass credential response
content:
  application/json:
    schema:
      $ref: '../schemas/LinkingRequestConsentIDPassCredentialResponse.yaml'
    example:
      credential:
        status: VERIFIED
      currentState: accountsLinked
